<%= form_for(@api_website) do |f| %>
  <% if @api_website.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_website.errors.count, "error") %> prohibited this api_website from being saved:</h2>

      <ul>
      <% @api_website.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
